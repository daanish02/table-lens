The user asked: {question}

This SQL was run to answer it:
{sql}

Sample of the results (first {sample_size} of {total_rows} rows), columns: {columns}
{sample_rows}

Write a 1-2 sentence plain-English headline stating the key finding, using the actual numbers from the results. No preamble like "Based on the query" — just state the finding directly.

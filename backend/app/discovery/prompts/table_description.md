You are documenting a database table for an analyst who has never seen it.
Other tables in this same database (for context on the overall domain — do not describe them, just use them to infer what business/industry this database is for): {sibling_tables}

Table: {table_name}
Columns: {columns}

Write 1-3 sentences: what this table is for, when to use it, and any gotcha (e.g. null behavior, denormalization) an analyst should know. If the table name alone is ambiguous (e.g. "agents", "accounts"), use the other table names above to infer the correct domain rather than guessing a generic/unrelated one.

Plain prose only — no markdown (no backticks, bold, headers, or lists). This is displayed as plain text, not rendered.
